# AtemStatus releases

Update feed for the [AtemStatus](https://github.com/steijehillewaert/AtemStatus) Mac app.
This repository holds only `appcast.xml` and the published builds — no source.

The app checks `appcast.xml` hourly through Sparkle and verifies every download
against the EdDSA public key baked into the app.
